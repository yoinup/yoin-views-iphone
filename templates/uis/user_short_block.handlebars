
{{#with view}}


	<div class="short-block-content">

		<div class="short-block-image">

			{{#if isYoinUser }}

				{{view Yn.SquareImageView class=imagetype
					contentBinding="content"
					withPhoto=true }}

			{{else}}

				{{#if isFbUser }}

					{{view Yn.SquareImageView class=imagetype
						contentBinding="fbId"
						isFBUser=true
						withPhoto=true }}

				{{else}}

					{{view Yn.SquareImageView class=imagetype
						contentBinding=null
						withPhoto=true }}

				{{/if}}

			{{/if}}

		</div>

		<div class="short-block-name">

			{{#if isYoinUser }}
			
			  <span class="main-text">
			  
			    {{#if content.suspended }}
		        {{I18n someone}}
		      {{else}}
		        {{content.name}}
		      {{/if}}

				</span>

				{{#if showCity}}

					{{#if content.city}}

						<div class="detail-text">

							<span class="icon with-text is-icon-x-small" icon="i">
							</span>

							{{content.city.name}}


						</div>

					{{/if}}

				{{else}}

						<div class="detail-text without-icon">
							{{content.status}}
						</div>

				{{/if}}

			{{else}}

				{{#if isFbUser }}

					<span class="main-text">{{fbName}}</span>

				{{else}}

					<span class="main-text">{{phoneName}}</span>

				{{/if}}	

			{{/if}}

		</div>

    {{#if hasInviteButton }}

      <div class="short-block-button">
      
        {{view Yn.AdaptativeButtonView class="is-blue button-text is-small is-font-white" 
          contentI18n="invite" 
          action="invite" 
          actionContentBinding="content"}}

      </div>

    {{/if}}	

</div>

{{/with}}
