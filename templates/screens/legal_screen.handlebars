{{#with view}}

{{#view Yn.HeaderView}}
  {{view Yn.BackButtonView }}
  <div class="header-title">

    {{#if isPrivacy }}
      {{I18n privacy_policy}}
    {{else}}
      {{I18n terms_and_conditions}}
    {{/if}}
  </div>
{{/view }}


{{#view Yn.ScrollerView class="screen-container"
  exclude=".header" }}
  
  {{template legal_container}}

{{/view }}

{{/with}}
